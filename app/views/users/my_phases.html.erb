

<div id="app">
    <div class="main-wrapper">

      <%= render "dashboard/header" %>
      <%= render "dashboard/navbar" %>

      <!-- Main Content -->
      <div class="main-content">
        <section class="section">
          <div class="section-header">
            <h1>My Phases</h1>
          </div>
          <div class="row">
            <div class="col-12">
              <div class="card">
                <div class="card-header">
                  <h4>Phases</h4>
                  <div class="card-header-form">
                    <form>
                      <div class="input-group">
                        <input type="text" class="form-control" placeholder="Search">
                        <div class="input-group-btn">
                          <button class="btn btn-primary"><i class="fas fa-search"></i></button>
                        </div>
                      </div>
                    </form>
                  </div>
                </div>
                <div class="card-body p-0">
                  <div class="table-responsive">
                    <table class="table table-striped">
                      <tr>
                        <th>Phase Type</th>
                        <th>Start Date</th>
                        <th>Due Date</th>
                        <th>Lead Name</th>
                        <th>Phase Status</th>
                        <th>Technincal Manager</th>
                        <th>Action</th>
                      </tr>
                      <% @phases.each do |phase| %>
                      <tr>
                        <td><%= phase.phase_type %></td>
                        <td>
                          <%= phase.start_date.strftime("%m-%d-%Y") %>
                        </td>
                        <td>
                          <%= phase.due_date.strftime("%m-%d-%Y") %>
                        </td>
                        <td>
                          <%= phase.lead.project_name %> <br>
                        </td>
                        <td><%= phase.phase_status %></td>
                        <td><%= phase.user.name %></td>
                        <td>
                          <%= link_to "Phase Details", phase_path(phase.id), class: "btn btn-primary" %>
                        </td>
                      </tr>
                      <% end %>
                    </table>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </section>
      </div>

      <%= render "dashboard/footer" %>

    </div>
  </div>